# MEDIUM Cost-Correctness Remediation Prompt

A single, code-grounded remediation brief for the **109 MEDIUM** findings surfaced by the all-services cost audit. Unlike the 29 CRITICALs, these are **audit-asserted, not adversarially verified** — sound but lower-stakes, and each must be re-confirmed against the current code before you touch it. They are clustered below by root cause (shared engine fix first, then ten per-pattern clusters). Paste the **PROMPT** section into a fresh session. *(Includes `network` NET-02, added after the `NetworkModule` gap-audit — Cluster E.)*

Scope is **strictly cost**: every emitted recommendation must produce a concrete, account-specific dollar saving. A finding that cannot be quantified from evidence becomes a **$0 advisory** (`Counted=False`, rendered-not-counted) — never a fabricated counted dollar.

---

## PROMPT (copy from here)

You are remediating **109 MEDIUM cost-correctness findings** in this AWS Cost Optimization Scanner. They are grouped into **1 shared root-cause fix** (`SR-1`, do FIRST) and **10 per-pattern clusters (A–J)**. Every item lists its `service Mx`, exact `file:lines`, the asserted defect, and the fix.

**These MEDIUM findings were NOT run through the adversarial verifier.** Before changing anything, re-read the cited `file:lines`, reproduce the defect against the *current* code, and discard or re-scope any item the code no longer matches. Line numbers are from the audit snapshot — re-anchor on surrounding code, not the absolute line. Treat every dollar/rate assertion below as a claim to validate, not a fact.

Work one cluster at a time, fully green before moving on. Do NOT batch unrelated edits into one commit. Many MEDIUM items share a shim/file with a CRITICAL fix for the same service — where noted, fold them into that service's CRITICAL commit rather than opening a second one.

### Global rules (apply to every fix)

1. **Confirm before editing.** Re-read the cited `file:lines` and reproduce the defect mentally against the current code first. These are unverified — if the asserted line/behavior is gone, skip it and say so.
2. **Validate every rate live.** Use the AWS Pricing MCP (`get_pricing` with the correct service code + pinned `usagetype`/`productFamily`/SKU filters) and the AWS Knowledge MCP. Never trust a hardcoded rate or memory. Record the validated value in the finding's `AuditBasis`.
3. **The advisory-$0 pattern is the default remedy for any saving you cannot quantify from evidence.** Mirror `services/adapters/lambda_svc.py` (metric-gated $0 advisory): set `Counted=False`, `EstimatedMonthlySavings=0.0`, and an honest `EstimatedSavings` string ("$0.00/month — advisory: …"). The rec still renders; it just does not feed the headline.
4. **Classify AWS errors, never swallow them.** Replace `except: pass` / `except: continue` / `logger`-only paths with `services/_aws_errors.record_aws_error(ctx, e, service=…, …)`: `AccessDenied`/`UnauthorizedOperation`/`OptInRequired` → `ctx.permission_issue`, everything else → `ctx.warn`. Reference uses: `services/adapters/network.py`, `services/rds.py`.
5. **Fail safe on safety-critical recs.** For any rec that recommends **deletion/termination/deregister**, a *failed* evidence read must **abstain** (do not emit), never assert "unused/delete".
6. **Immutability.** Build new rec dicts; do not mutate shared inputs in place.
7. **Counted == rendered.** The number summed into `total_monthly_savings` must equal the dollar shown on the card, and a `$0`/advisory rec must be `Counted=False` so the "N (X counted, Y advisory)" header reconciles. Kill every string-vs-number desync.
8. **Each counted finding carries a structured `AuditBasis`** (rate / region / metric-window / formula) so the dollar is defensible from the report alone.
9. **Tests + regression gate.** For each fix, add/extend a `tests/test_<svc>_audit_fixes.py` mirroring `tests/test_lambda_audit_fixes.py` / `tests/test_rds_audit_fixes.py`: drive the pure logic and the `scan()` path with a `SimpleNamespace` ctx + fake boto3 paginators. Keep the gate green: `pytest tests/test_regression_snapshot.py tests/test_reporter_snapshots.py`. Rate corrections (Cluster D), advisory demotions / counted==rendered fixes (Cluster F), and SR-1 **will** shift golden/reporter snapshots — refresh with `SNAPSHOT_UPDATE=1` **only** after eyeballing the diff and confirming it is the intended dollar change, and say so in the commit.
10. **Stage only the files you changed.** One cluster → one focused commit.

### Sibling reference patterns (study before fixing)

- **CoH consumption + CoH>heuristic dedup by normalized id:** `services/rds_logic.py`, `services/adapters/rds.py` (`ctx.cost_hub_splits["rds"]`).
- **Metric-gated $0 advisory + arch-aware constant:** `services/adapters/lambda_svc.py`.
- **Exact current→target price delta (not a reduction factor):** `services/adapters/ec2.py`, `services/ebs.py`.
- **Deterministic, usagetype-filtered Pricing lookup:** `core/pricing_engine.py` `_select_efs_storage_rate` (≈1346-1374) and the hourly unit-guarded path `_call_pricing_api_hourly`.
- **AWS error classification:** `services/_aws_errors.py` `record_aws_error` (`is_permission_error` ≈47-56).
- **Structured `AuditBasis` per counted rec:** `services/rds.py:585-600,651-661`.
- **Advisory-as-counted reconciliation:** the reporter counts any rec **without** `Counted is False` as counted (`html_report_generator.py:371-388` `_counted_advisory_counts`); the generic per-rec card prints `rec['EstimatedSavings']` verbatim (`reporter_phase_b.py` `_render_generic_other_rec`). The fix is always in the adapter (set `Counted=False` and/or make the string equal the counted dollar), never in the reporter math.

---

## SHARED ROOT-CAUSE FIX (do first)

### SR-1 — `core/pricing_engine.py`: pricing-miss and non-deterministic SKU paths silently return $0 / wrong rates
**Repairs:** SageMaker **M4, M5**, OpenSearch **M4**. Shares the `MaxResults=1` mechanism with **CRITICAL SR-1** — do the `component`/usagetype pin in the same engine pass.

- **sagemaker M5** — `core/pricing_engine.py:1282-1288, 1590-1613`. `_fetch_sagemaker_instance_price` filters only `instanceType + location`; `_call_pricing_api` uses `MaxResults=1` → `PriceList[0]`. AmazonSageMaker exposes a `component` attribute (Hosting/Notebook/Training/Processing/Reserved/`*-Free-Tier`/…) with many SKUs per type; today they all price equal so it is harmless, but a `$0` Free-Tier/Reserved row serializing first → `_extract_usd` None → fallback → $0, and the family is conceptually wrong. **Fix (fold into CRITICAL SR-1):** pin `component` per call site — Hosting for endpoints, Notebook for notebook instances, Training for training jobs — so the lookup is deterministic and family-correct; reject `$0` rows. *Covered under CRITICAL SR-1 — check the deterministic-selection box there; this adds the per-call-site `component` pin.*
- **sagemaker M4** — `core/pricing_engine.py:939-956, 1067-1085`. `get_sagemaker_instance_monthly` falls back to `get_ec2_hourly_price(instance_type)*SAGEMAKER_OVER_EC2*730`, but `instance_type` is an `ml.*` string passed verbatim as `instanceType`; AmazonEC2 has no `ml.m5.xlarge` SKU → returns 0.0 → `fallback = 0.0`. Idle endpoint/notebook checks then count a $0 finding. **Fix:** strip the prefix before the EC2 fallback (`get_ec2_hourly_price(instance_type.removeprefix("ml."))`); recalibrate `SAGEMAKER_OVER_EC2` toward ~1.20 (live: EC2 m5.xlarge $0.192 vs SageMaker ml.m5.xlarge $0.23).
- **opensearch M4** — `services/adapters/opensearch.py:61-64,87` + `core/pricing_engine.py:740-752`. `get_instance_monthly_price` returns `_use_fallback(0.0,…)` on a Pricing-API miss (no region-scaled constant); a Graviton rec priced 0.0 is then dropped by the `keep>0` guard with no warning, so a transient miss silently erases a real counted saving. **Fix:** distinguish a pricing miss (`ctx.warn`/advisory) from a true $0; add a region-scaled fallback constant for the AmazonES instance path.

---

## CLUSTERS

### Cluster A — Silent failures / error-swallowing (classify, don't swallow; fail safe)

Shared remedy = Global rule #4 (`record_aws_error` → `ctx.permission_issue`/`ctx.warn`). Never let a permission/throttle read like "no data / no resources / idle". On a **deletion** rec, abstain on read failure (rule #5).

- **ami M2** — `services/ami.py:39-49`. `_snapshot_storage_gb` inner `except` does `logger.warning` then `total_gb += ebs.get("VolumeSize", 8)` — a denied `DescribeSnapshots` silently substitutes provisioned size (~2× the stored blocks) into a **counted** dollar with no ctx signal. **Fix:** classify on ctx; skip the AMI (or mark basis `unpriced`) rather than fabricating a `VolumeSize`/8 GB figure.
- **aurora M3** — `services/adapters/aurora.py:76-91,104-119,128-145`. `_get_cloudwatch_avg/_sum/_avg_max` wrap `get_metric_statistics` in `try/except Exception: pass; return None`, so AccessDenied/throttle is indistinguishable from empty datapoints and silently suppresses the serverless-ACU and I/O-tier checks. **Fix:** pass `ctx`, classify ClientError codes, return None only for genuinely empty Datapoints.
- **bedrock M1** — `services/adapters/bedrock.py:50-55,80-81,107-108,122-123,232-233,247-248,277-278,292-293,348-352`. Every list/metric/agent path swallows the exception and returns `[]`/None/`continue`; a permission gap yields an empty or KB-only tab with zero operator signal. **Fix:** classify each `except`; distinguish "no metrics" (idle) from "couldn't read metrics" before deciding idle.
- **commitment_analysis M2** — `services/adapters/commitment_analysis.py:803-820`. `_account_coverage_ratio` uses a bare `except Exception: return DEFAULT_COVERAGE_RATIO` (0.70) while every other CE call routes through `_route_ce_error`. **Fix:** `except Exception as e: _route_ce_error(ctx, "ce:GetSavingsPlansPurchaseRecommendation[account_coverage]", e); return DEFAULT_COVERAGE_RATIO`.
- **dynamodb M2** — `services/dynamodb.py:201-203, 371-372, 488-503`. Bare `except Exception:` around `get_metric_statistics`; on the provisioned path it still appends the over-provisioned 0.40 rec (counted) with no metric evidence and no ctx signal. **Fix:** catch ClientError, route AccessDenied/Unauthorized/OptInRequired → `ctx.permission_issue`, throttling → `ctx.warn`; do not count a capacity rec whose CW read failed.
- **ebs M1** — `services/ebs.py:647-648`. The outer `try` over gp2 scan + IOPS rightsizing + snapshots ends in a generic `ctx.warn`; a denied `DescribeVolumes`/`DescribeSnapshots`/`DescribeImages` aborts the whole enhanced path with no permission_issue. **Fix:** split into `except ClientError` classifying Unauthorized/AccessDenied → `ctx.permission_issue`, keep a trailing `except Exception`; mirror `get_unattached_volumes` (322-329).
- **ebs M2** — `services/ebs.py:60-62, 475-482`. `_observed_peak_iops` does `logger.warning; return None`; the caller then prints per-volume "no CloudWatch IOPS data", so a CW AccessDenied/throttle mass-suppresses IOPS rightsizing while reporting it as benign empty-metric. **Fix:** catch ClientError separately → permission_issue/warn (once), return a sentinel distinct from empty datapoints.
- **ec2 M1** — `services/ec2.py:803-814`. The burstable `CPUCreditBalance` non-permission branch is `logger.debug` only, dropping a real counted finding with no warning, while the sibling CPU lookup (662-675) calls `ctx.warn`. **Fix:** replace both `logger.debug` calls with `ctx.warn(... burstable check skipped, service="ec2")`.
- **eks_cost M1** — `services/adapters/eks.py:514-518`. `_build_cost_hub_recs` does `except Exception: return recs` — a missing/malformed `ctx.cost_hub_splits` silently drops **counted** EKS CoH dollars. **Fix:** `ctx.warn(f"EKS Cost Hub split read failed: {e}", "eks_cost")` before returning.
- **elasticache M1** — `services/elasticache.py:159-161`. CW block ends `logger.warning(...); continue`, losing the only evidence-gated counted lever (Underutilized) on a CW denial. **Fix:** classify via `record_aws_error`.
- **elasticache M4** — `services/elasticache.py:163-164`. Outer guard is a generic `ctx.warn`; a `DescribeCacheClusters` AccessDenied that zeros the tab is reported as an ordinary warning. **Fix:** route through `record_aws_error`.
- **lightsail M3** — `services/lightsail.py:49-104`. A single `try` wraps both `get_instances` pagination and `get_static_ips` with one generic `ctx.warn`; a static-IP permission gap loses the already-collected instance findings. **Fix:** per-call `try` blocks + classify via `record_aws_error`; keep partial results.
- **mediastore M4** — `services/mediastore.py:27-29, 110-115`. Outer + inner `ctx.warn` never classify AccessDenied on `ListContainers`/metric reads. **Fix:** replace with `record_aws_error(ctx, exc, service="mediastore", …)`. *(Do alongside mediastore CRITICAL C1 — same shim.)*
- **monitoring M2** — `services/backup.py:112,115,122`; `services/route53.py:127,146,178`. Every `except` calls `ctx.warn` regardless of code; `ListBackupPlans`/`ListHostedZones` AccessDenied under-reports the permission ledger. **Fix:** classify each code via `record_aws_error`.
- **msk M3** — `services/msk.py:80-81, 83-84`. Outer generic `ctx.warn` for `kafka:ListClusters` AccessDenied, plus inner `list_clusters_v2` `except: pass`. **Fix:** classify outer; replace inner `except: pass` with at least `ctx.warn`.
- **opensearch M2** — `services/opensearch.py:166-168, 170-171, 173-174`. Inner `describe_domain`/CW excepts are `logger.warning` only → a denied domain VANISHES from the report; outer `ctx.warn` doesn't classify. **Fix:** route all three through `record_aws_error`; never let a domain disappear logger-only. *(Do with opensearch CRITICAL C2.)*
- **redshift M3** — `services/redshift.py:81-103`. The redshift-serverless `list_workgroups` loop is `try: … except Exception: pass` — a denial/throttle silently no-ops the whole serverless path. **Fix:** classify via `record_aws_error`; add `redshift-serverless` to `required_clients()`. *(Do with redshift CRITICAL C1.)*
- **s3 S3-N1** — `services/s3.py:945-960, 791-796`. Per-class `BucketSizeBytes` loop and `_assess_bucket_coldness` route only endpoint-unreachable errors to ctx; CW AccessDenied/Throttling fall to `logger.debug`+continue, silently zeroing `total_size_gb` and degrading coldness to "unknown". **Fix:** classify both metric-read sites (AccessDenied/Unauthorized/OptInRequired → permission_issue; Throttling/RequestLimitExceeded → warn; keep endpoint-unreachable → `_mark_region_dead`).
- **sagemaker M1** — `services/adapters/sagemaker.py:44-50, 64-79, 82-95, 98-111, 169-170, 191-192, 214-215, 239-240, 252-256, 300-301, 335-336`. Every helper/loop swallows via `except Exception: pass/continue/return None`; a `ListEndpoints`/`ListNotebookInstances`/CW denial reads as "no resources/idle" → false clean bill. **Fix:** wrap list/describe/CW calls and classify via `record_aws_error`.
- **step_functions M1** — `services/step_functions.py:78-79` (+ `services/adapters/step_functions.py:70`). Inner per-machine `except: pass` and adapter `except → monthly_executions=0` silently drop high-volume Standard machines; outer `ctx.warn` doesn't classify. **Fix:** classify all three via `record_aws_error`. *(Do with step_functions CRITICAL C1.)*
- **transfer M1** — `services/transfer_svc.py:86-91`. Inner CW `except` writes a cosmetic `DataTransferCostNote` instead of classifying. **Fix:** route to `record_aws_error` (`cloudwatch:GetMetricStatistics`).
- **transfer M2** — `services/transfer_svc.py:105-106`. Outer `except` always `ctx.warn`; `transfer:ListServers` AccessDenied never becomes a permission_issue. **Fix:** classify the code. *(Do with transfer CRITICAL C1.)*

### Cluster B — Fabricated $ from a config dimension with no usage metric (gate on a real metric or demote to $0 advisory; mirror `lambda_svc.py`)

- **apprunner M4** — `services/adapters/apprunner.py:14, 38-49, 64-72`. `DEFAULT_ACTIVE_HOURS_PER_MONTH=160` ("8h×20d") anchors every active-monthly term; the CW scaling branch is dead (its own H1), so `active_hours` is always 160 — fabricated. **Fix:** derive active hours from per-service `RequestCount`/`ActiveInstances`; unavailable or fast_mode → $0 advisory with `AuditBasis`.
- **athena M1** — `services/adapters/athena.py:66-71`. Unconditional `*0.75` compression factor applied to all scanned bytes with no current-format detection → fabricates a 75% saving for already-Parquet/ORC workgroups. **Fix:** detect current format (table input/output format) and gate the 0.75 behind evidence the data is non-columnar, else $0 advisory; record the factor in `AuditBasis`. *(Latent behind athena CRITICAL C1 until the shim emits recs.)*
- **msk M4** — `services/msk.py:42`. `if state=="ACTIVE" and instance_type and "large" in instance_type:` flags every large+ broker at a blanket 30% with no utilization, misses `t3.small`/small-graviton, and skips non-ACTIVE. **Fix:** gate on a CloudWatch utilization signal (BytesIn/Out, CPU) before recommending downsize; no metric → $0 advisory; document the ACTIVE-only scope. *(Compounds msk CRITICAL C1.)*
- **workspaces M2** — `services/workspaces.py:149-151`. Recommendation text claims "based on utilization profile" while the adapter reads only `describe_workspaces` config (no CW). **Fix:** gate on AWS/WorkSpaces CPU/session metrics, or reword to a config-only advisory and demote to `Counted=False` until evidence exists.

### Cluster C — Reduction-factor instead of exact current→target delta (compute the real delta; mirror `ec2.py`/`ebs.py`)

- **apprunner M5** — `services/adapters/apprunner.py:15, 99`. `savings += monthly_cost * 0.12` flat over the whole modeled cost. **Fix:** compute `current_cost − target_cost` from the next-smaller valid Cpu/Memory config at $0.064/vCPU-hr + $0.007/GB-hr.
- **batch M1** — `services/adapters/batch.py:48-61`; `services/batch_svc.py:68-78`. Graviton saving = `price(instance_types[0])×730×0.20`; only `instance_types[0]` is priced and there is no arm-equivalent existence check (`optimal`/family labels price to $0). **Fix:** exact `price(x86)−price(arm_equivalent)` against a real map (m5→m6g, c5→c6g, r5→r6g…); skip when no arm equiv; price across the CE's actual mix; add `AuditBasis`. *(Honesty piece overlaps batch CRITICAL C1 — check off there.)*
- **ec2 M4** — `services/ec2.py:32, 707-723`. Dedicated-tenancy saving counts `hourly×730×0.30` of full on-demand; for Dedicated *Instances* the per-instance rate equals shared and the premium is largely a per-region $2/hr host fee, so 0.30 overstates. **Fix:** derive an exact tenancy=Shared vs dedicated delta (or amortize the per-region fee), else demote to advisory.
- **elasticache M2** — `services/adapters/elasticache.py:47, 60-62`. Underutilized counts `monthly_node×NumNodes×0.30` regardless of feasible steps down. **Fix:** pick a target node type from CPU headroom (bounded by `SMALLEST_SIZES`), price both via the fixed pricing path (CRITICAL ElastiCache C2), count `(price_current−price_target)×NumNodes`; record the target in `AuditBasis`.
- **glue M1** — `services/adapters/glue.py:42-44, 72`. `monthly_cost*0.30` flat (card string separately claims "20-40%"), no target DPU. **Fix:** `(current_DPU − target_DPU)×rate×hours` against an auto-scaling floor or observed peak from `get_job_runs`.
- **opensearch M3** — `services/adapters/opensearch.py:46-49, 61-64`. Flat `0.25` Graviton / `0.30` downsize with no target instance and no `AuditBasis`. **Fix:** `price(x86)−price(graviton-equiv)` and `price(current)−price(one-size-down)` via `get_instance_monthly_price`; else $0 advisory. *(Do with opensearch CRITICAL C2.)*
- **rds M1** — `services/rds.py:68, 564-568`. `RDS_NON_PROD_SCHEDULE_REDUCTION=0.65` counted, gated on genuine idle evidence (`DatabaseConnections<=1.0` 14d) but not the user's actual schedule. **Fix:** keep the idle gate; present scheduling as a range or clearly label 0.65 a planning factor in `AuditBasis`; optionally demote unless a configured schedule is known. Acceptable as a documented tradeoff.

### Cluster D — Wrong rate / SKU / unit / region-scaling / metric-math (pin the SKU; respect region-flat rates; validate metric semantics)

- **api_gateway M1** — `services/api_gateway.py:14-16, 91-93`. `SAVINGS_PER_M = 3.50 − 1.00 = 2.50/M` applied flat; live tiers make the true REST→HTTP delta $1.90 or $1.48 beyond the first tier, so >333M-req APIs overstate ~24-41%. **Fix:** apply the tiered REST/HTTP schedules, or label $2.50/M a first-tier upper bound in `AuditBasis`.
- **aurora M1** — `services/adapters/aurora.py:22,43`. `ACU_HOURLY_FALLBACK=0.06` is 2× low vs live `$0.12` and disagrees with `core/pricing_engine.py:301` (0.12). **Fix:** set `0.12` (region-scaled once via multiplier). *(Pair with aurora CRITICAL C1.)*
- **aurora M5** — `services/adapters/aurora.py:348-356`. `monthly_io` = `Sum(VolumeReadIOPs+WriteIOPs)/14*30`; if those metrics surface as an averaged rate, the daily Sum is a meaningless sum-of-averages and the whole io_tier saving is fabricated. **Fix:** validate via CloudWatch metric metadata that daily Sum = total billed ops; otherwise compute ops from `Average×period-seconds` and base the month on the observed window, not `/14*30`.
- **athena M2** — `services/adapters/athena.py:36-69`. Flat `$5/TB` applied to every workgroup; provisioned-capacity ($0.30/DPU-hr) and Spark ($0.35/DPU-hr) workgroups have no per-TB scan charge. **Fix:** read workgroup Configuration (engine/version, provisioned-capacity assignment); skip or DPU-price those instead of $5/TB.
- **dynamodb M1** — `services/dynamodb.py:184-195`. On-demand cost uses the `Average` of `Consumed*CapacityUnits` over `Period=3600` × 730 — multiplying a per-sample average by hours (not `730×60`) understates monthly volume ~60×, and capacity-units ≠ request count. **Fix:** use `Statistics=['Sum']` over the window × `30/lookback_days` for true monthly request units; do not multiply a 1-hour Average by 730.
- **ebs M3** — `services/ebs_logic.py:127-140`. `gp2_to_gp3_net_savings` nets only IOPS parity, not throughput; gp2 ≥170 GB delivers >125 MiB/s baseline, so the migration line is overstated ~4× at 334 GB, ~33% at 1 TB. **Fix:** subtract `max(0, gp2_baseline_throughput(size)−125)×get_ebs_throughput_monthly_price("gp3")`; record in `AuditBasis`.
- **ec2 M2** — `services/ec2.py:344-364, 1097-1118`. `describe_spot_price_history(MaxResults=1)` returns whichever AZ AWS lists first, feeding a **counted** `(on_demand−spot)×730` that is non-reproducible. **Fix:** pass the instance's AZ + a multi-point window (e.g. `StartTime=now-7d`) and average/take a conservative max, or demote spot_migration to advisory. (Mirrors SR-1's non-determinism theme.)
- **eks_cost M2** — `services/adapters/eks.py:415-427 / 339-353`. `_node_group_monthly_cost` prices `instance_types[0]` as Linux (no `os` arg) on `desiredSize`, ignoring `amiType` (Windows/BYOL) and multi-type groups. Advisory-only ($0 headline) but the displayed $ misleads. **Fix:** derive `os` from `ng.amiType`; note the basis is `desiredSize × first type`.
- **file_systems M1** — `services/efs_fsx.py:318-333`. Idle-EFS delete prices ALL bytes (`size.Value` across Standard+IA+Archive) at the Standard rate; live IA $0.025 (12× lower), Archive $0.008 (37× lower), so a tiered idle FS over-counts up to ~37×. **Fix:** price per class — `ValueInStandard×std + ValueInIA×ia + ValueInArchive×archive` (+ One-Zone variants); the sub-fields and `_efs_rate(class)` are already in scope.
- **monitoring M1** — `services/route53.py:88-90, 162-164`. Route 53 zone savings `×pricing_multiplier`, but hosted-zone pricing is GLOBAL (`$0.50`/`$0.10`, SKU KVPGEJE88UW8S779), inflating non-us-east-1 by the regional premium. **Fix:** pass `1.0`; add `AuditBasis region="global"`.
- **network_cost M1** — `services/adapters/network_cost.py:32, 398, 406, 410, 428`. `TGW_ATTACHMENT_COST_PER_GB=0.05` is actually `$0.05/attachment-HOUR` (SKU 9N2FHGMMCZBKDCGH, ~$36.50/mo); only `$0.02/GB` is processing. The code sums them into a `$0.07/GB` display string. No counted-$ impact (advisory branches), but the operator-facing value is wrong. **Fix:** rename to `TGW_ATTACHMENT_COST_PER_HOUR`, report `0.05*730*attachments` separately from the $0.02/GB rate.
- **quicksight M1** — `services/adapters/quicksight.py:46-50`. Saving = `rate*(total−used)` counts all idle SPICE headroom, ignoring the per-author/reader included free allotment, overstating by up to `included_free_GB*rate`. **Fix:** floor to `max(0, total−used−included_free_GB)*rate` from author/reader counts; record the allotment in `AuditBasis`. *(Do with quicksight CRITICAL C1.)*
- **sagemaker M2** — `services/adapters/sagemaker.py:148-155, 331, 349`. `idle_endpoints`/consolidation price only `variants[0].InstanceType` × one instance, ignoring `InstanceCount` and additional ProductionVariants → understates multi-instance/multi-variant endpoints. **Fix:** sum price over all variants × each `InstanceCount`.
- **workspaces C7** — `services/workspaces.py:29-37`. `WORKSPACE_BUNDLE_MONTHLY` `POWERPRO=124` / `GRAPHICSPRO=735` understate live `$140` / `$999` by 11% / 26%. **Fix:** refresh from live AlwaysOn/Windows/Included rows (PowerPro 140, GraphicsPro 999, GraphicsPro.g4dn 959) with a dated provenance comment; re-check STANDARD (load-bearing for billing_mode/unused counts).

### Cluster E — Duplication: intra-adapter stacking, cross-source, cross-adapter (dedup; cap at monthly cost)

- **ami M1** — `services/ami.py:38,143-152`. No global `seen_snapshot_ids` set across the AMI loop; two emitted AMIs sharing a backing `SnapshotId` count that storage twice. **Fix:** track a module-level seen-set, add a snapshot's GB to savings only on first encounter (still list it under each AMI).
- **batch M3** — `services/batch_svc.py:55-78`. Spot and Graviton branches are independent `if`s on the same non-Fargate CE; harmless today (only Graviton counts) but wiring Spot would discount one CE by 0.70 then 0.20. **Fix:** before enabling Spot, define authority order (Spot on full, Graviton on remainder) or one rec per CE; add a per-CE `covered` guard. *(Pre-empt with batch CRITICAL C1.)*
- **batch M4** — `services/adapters/batch.py:67-75`; `core/result_builder.py`. Batch EC2-CE instances (tagged `AWSBatchServiceTag`) can be counted both as a Batch Graviton saving and as an EC2 rightsizing/Spot saving across tabs. **Fix:** pick one owner — exclude `AWSBatchServiceTag` instances from the EC2 adapter (CE-level config is the Batch lever) and add to the shared dedup set.
- **containers M3** — `services/containers_logic.py:269-277, 296-311`. `normalize_resource_name` collapses ECS ARNs to the last path segment, so `clusterA/web` and `clusterB/web` both key to `web` → a CoH rec for one suppresses the genuine heuristic rec for the other. **Fix:** make the dedup key cluster-qualified (`cluster/service`) across coh_key/co_key/heuristic_key.
- **dms M1** — `services/adapters/dms.py:39-48 + 52-92`. A non-prod Multi-AZ idle instance is counted in rightsizing (`×0.35`, plus the unused bucket via C1) AND multi_az (`×0.5`) → up to 1.2× the monthly bill. **Fix:** single owner per instance — compute rightsizing against the post-Single-AZ baseline, or make Multi-AZ removal exclusive of rightsizing. *(Do with dms CRITICAL C1.)*
- **ec2 M3** — `services/adapters/ec2.py:37-45, 129-132`. `_coh_instance_id` returns `resourceId` verbatim while `_co_instance_id` strips the ARN; if CoH ever returns ARN-form ids the CoH>CO dedup fails → 2× count. **Fix:** one `_normalize_instance_id` (strip any `/` suffix) applied to both sides.
- **lambda M1** — `services/adapters/lambda_svc.py:112-138`. `seen_functions` (CoH + kept CO recs) drops ALL enhanced recs for a colliding function, but CoH/CO are memory-only — the counted Provisioned-Concurrency rec (a distinct 24/7 lever) is silently dropped with its card. **Fix:** exempt the PC category from the cross-source filter (only dedup Excessive-Memory/ARM enhanced recs); add a test: CO memory rec + PC config → both counted.
- **workspaces C6** — `services/workspaces.py:96,122-156`. The `AVAILABLE&&ALWAYS_ON` gate (96) and `AVAILABLE&&rank>=3` gate (122) aren't exclusive; one POWER ALWAYS_ON WorkSpace counts billing_mode ($10.50) AND bundle_rightsizing ($20) = $30.50 against an ~$80 bill. **Fix:** one lever per WorkSpace (rightsize first, then AutoStop on the smaller bundle) or a combined non-additive saving; add to a `covered` set after the first counted rec. *(Depends on the workspaces billing-mode "C3" 0.30-fallback fix — track together.)*
- **network NET-02** — `services/vpc_endpoints.py:111-125` (`interface_endpoints_in_nonprod`) and `132-150` (`duplicate_endpoints`). A dev/test-tagged interface endpoint that is also the 3rd+ of its `vpc:service` group is counted in BOTH checks (each at `vpc_ep_monthly × az_count`, ≥ $7.30/AZ) → over-counts the VPC-endpoint domain by that amount per overlapping endpoint (common in a nonprod VPC with >2 same-service endpoints). **Fix:** make the checks mutually exclusive — track `VpcEndpointId`s already emitted by `interface_endpoints_in_nonprod` and exclude them from `duplicate_endpoints` (count each endpoint once; attach the nonprod reasoning as metadata, not a second counted finding).

### Cluster F — String-vs-counted desync & advisory-rendered-as-counted (counted == rendered)

Shared anchors: `_counted_advisory_counts` counts any rec without `Counted is False`; the generic per-rec card prints `rec['EstimatedSavings']` verbatim. Fix in the adapter.

- **aurora M2** — `reporter_phase_b.py:1812-1876`. Aurora has no PHASE_B handler; `serverless_v2`/`io_tier` recs carry no `EstimatedSavings`/`CheckCategory`/`cluster_id`, so the generic loop dumps `Monthly Savings: 1051.2` (raw float, no `$`, no styling, resource id `Resource`). **Fix:** give those recs an `EstimatedSavings` string (`f"${monthly_savings:.2f}/mo"`) + `CheckCategory` + `resource_id=cluster_id`, or register a dedicated handler.
- **cloudfront M1** — `reporter_phase_a.py:450-451`; `services/cloudfront.py:76`; `services/adapters/cloudfront.py:60-64`. Adapter forces `EstimatedMonthlySavings=0.0` + `PricingWarning` but never clears the helper's `EstimatedSavings='20-50% …'`, so a $0/advisory tab shows a savings claim with no dollar and no caveat (PricingWarning never rendered). **Fix:** drop the percentage string (set it to `''` or the PricingWarning text), or render PricingWarning when savings==0; reject non-dollar strings from the savings line.
- **commitment_analysis M1** — `services/adapters/commitment_analysis.py:150`; `html_report_generator.py:3181-3186`. `total_recommendations = len(all_recs)+len(cost_hub_recs)` excludes `fargate_sp_recs` that still render and are still counted by `_counted_advisory_counts`, so `X counted + Y advisory > N` whenever Fargate cells exist. **Fix:** pick one source of truth — include Fargate in `total_recommendations` or exclude Fargate/CoH-only sources from the count.
- **dms M2** — `services/adapters/dms.py:76-91`; `reporter_phase_a.py:185-196`. multi_az recs use key `Resource` (no `InstanceId`), so `_extract_dms_details` renders every card as `Unknown (<class>)` — the counted Multi-AZ saving is unactionable. **Fix:** add `InstanceId: name` to the rec, or fall back to `rec.get("Resource")`.
- **dynamodb M4** — `reporter_phase_b.py:909-912`. The adapter counts `0.30*monthly_current` for any provisioned table with `rcu>0`, including well-sized tables with no opportunities; those recs have no `CheckCategory`/opportunities → `else: continue`, so they're counted but render no card (and may drop the whole source). **Fix:** only count a `table_analysis` rec carrying a concrete rendered remediation (gate the 0.30 behind a real opportunity), or render a card for every counted rec.
- **elasticache M3** — `reporter_phase_b.py:1052-1089` (esp. 1071-1073). `_render_elasticache_enhanced_checks` prints the shim's raw percentage string (`"20-40%"`, etc.) and never `EstimatedMonthlySavings`; advisory cards render identical strings with no counted/advisory marker. **Fix:** render each rec's dollar + a `Counted`-driven marker; relabel the percentage as a list/marketing range; visually separate `Counted==False`.
- **glue M3** — `services/adapters/glue.py:53-64`. When `dpu_count` is None/0 the adapter sets `EstimatedMonthlySavings=0.0` + `PricingWarning` but not `Counted=False`, so $0 dev-endpoint/capacity-less recs inflate "X counted". **Fix:** set `rec['Counted']=False` on the $0+PricingWarning path.
- **mediastore M1** — `services/mediastore.py:91-93,106`; `services/adapters/mediastore.py:48-49`; `reporter_phase_b.py:1874-1875`. Adapter overwrites `EstimatedMonthlySavings` with the region-correct rate but not `EstimatedSavings`, so the card shows the helper's `0.023*multiplier` string while the headline uses the engine rate. **Fix:** rewrite `rec['EstimatedSavings']=f"${rec['EstimatedMonthlySavings']:.2f}/month"`. *(Do with mediastore CRITICAL C1.)*
- **msk M1** — `services/msk.py:61-70`; `services/adapters/msk.py:44,62`. The volume>1000 rec carries `VolumeSize` but no `InstanceType` → $0, yet stays in `total_recommendations` with a "20% with gp3" string and is never `Counted=False`. **Fix:** mark it `$0` advisory via `mark_zero_savings_advisory`, or quantify gp2 $0.10 vs gp3 × VolumeSize × brokers under one owner; pre-empt the rightsizing+storage double-count with msk CRITICAL C1.
- **redshift M1** — `services/redshift.py:70-79`. `cluster_rightsizing` rec lacks `NodeType` (uses `CurrentNodes`) → adapter contributes $0, but renders `$(nodes-2)*100` and isn't `Counted=False`, inflating the counted tally. **Fix:** mark `Counted=False`, or carry `NodeType`+`NumberOfNodes` and compute `node_price*(CurrentNodes-target)`. *(Do with redshift CRITICAL C1.)*
- **redshift M2** — `services/redshift.py:91-101`. Serverless rec has no `NodeType` → $0 counted but renders `$150/month` and isn't `Counted=False`. **Fix:** mark `Counted=False` with `"$0.00/month — advisory (RPU pricing not modeled)"`, or price RPU-hours from CloudWatch.
- **workspaces C4** — `services/workspaces.py:104,117`. `billing_mode` rec shows `"$50/month potential"` (in no price table) and `unused` shows `"Full workspace monthly cost"`, while the headline counts $10.50 (the 0.30 fallback). **Fix:** set `EstimatedSavings`/`EstimatedSavingsAmount` to the same number `scan()` counts (after the "C3" fix); remove the fabricated $50 and "Full cost" strings.

### Cluster G — Coverage: pagination, hardcoded allowlists, whole-class skips, zero/idle resources

- **api_gateway M2** — `services/api_gateway.py:78-87`. `Count` queried on `ApiName` only requires per-stage Detailed Metrics (off by default) and is non-unique; empty datapoints → `monthly_requests=0` → the $0→$50 fabrication. **Fix:** when datapoints are empty, emit a `Counted=False` advisory; note the ApiName-collision risk in `AuditBasis`. *Covered under CRITICAL SR-2 / api_gateway C1 for the $50 piece — check off there.*
- **api_gateway M3** — `services/api_gateway.py:66-69`. `get_resources` is single-call (page size 25) and the `<=10` cutoff silently excludes high-cost 11+-resource APIs from the only savings check. **Fix:** paginate via `position`; replace/justify the `<=10` threshold with a feature-compatibility signal.
- **apprunner M1** — `services/apprunner.py:38`. `if status=="RUNNING":` skips PAUSED services that still bill provisioned memory ($0.007/GB-hr, ~$20.44/mo for 4 GB). **Fix:** inspect PAUSED/provisioned-idle services; emit a delete/resume rec priced at `mem_gb*0.007*730*multiplier`. *(Latent behind apprunner CRITICAL C1.)*
- **apprunner M2** — `services/apprunner.py:24-28, 56-58`. `unused_services` bucket declared, never populated; no zero-request check despite CW `RequestCount`. **Fix:** CW-gated `RequestCount==0` over N days → delete/pause rec at full provisioned+active cost; counted only with metric evidence, else $0 advisory.
- **apprunner M3** — `services/apprunner.py:31-32`. `list_services()` single call, no `NextToken` loop. **Fix:** use the ListServices paginator.
- **athena H3** — `services/athena.py:28-29`. `list_work_groups()` single call (50/page). **Fix:** paginate before building recs. *(Coverage realized once athena CRITICAL C1 emits recs.)*
- **batch M2** — `services/batch_svc.py:85-89`. Job rightsizing reads only legacy `containerProperties.vcpus/memory`; Fargate/ECS `resourceRequirements` and multi-node `nodeProperties` score 0. **Fix:** sum VCPU/MEMORY `resourceRequirements` and `nodeProperties.nodeRangeProperties[].container` before the `>8 vCPU / >16384 MB` gate.
- **containers M1** — `services/containers.py:416, 424-429`. Fargate rightsizing `continue`s when Container Insights is disabled (the common case) — no rec, not even an advisory, though the reporter has a dedicated "ECS Container Insights Required" group. **Fix:** when Fargate services run but CI is off, emit a `Counted=False` advisory with `CheckCategory="ECS Container Insights Required"`.
- **glue M2** — `services/glue.py:37`. `if max_capacity>10 or number_of_workers>10:` is the only path that appends a rightsizing rec, hiding fleets of small over-provisioned jobs. **Fix:** lower/remove the threshold and gate on actual utilization (peak vs allocated DPU from `get_job_runs`).
- **glue M4** — `services/glue.py:50-51`. `get_dev_endpoints()` single call, unlike paginated `get_jobs`/`get_crawlers`. **Fix:** use `get_paginator("get_dev_endpoints")`.
- **lightsail M1** — `services/lightsail.py:41-47, 49-101`. Only `get_instances` + `get_static_ips` are called; `load_balancer_optimization`/`database_optimization` keys are declared but empty — DBs, LBs, disks, snapshots never scanned. **Fix:** implement scans for `get_relational_databases`/`get_load_balancers`/`get_disks`/snapshots (each with a live rate + `AuditBasis`), or remove the dead keys and document scope.
- **lightsail M2** — `services/lightsail.py:73`. Oversized gated to `"large"`/`"xlarge"` substring (and `"large"` ⊂ `"xlarge"`), so nano/micro/small/medium and gen-3 names are never evaluated. **Fix:** drive rightsizing from a CW utilization signal across all running bundles (or document the allowlist); drop the redundant `"xlarge"` clause. *(Do with lightsail CRITICAL C1.)*
- **network_cost M2** — `services/adapters/network_cost.py:361-371`. `describe_vpc_peering_connections`/`describe_transit_gateways` are single calls; `tgw_count` has no state/owner filter (counts RAM-shared/deleting TGWs). **Fix:** paginate both; filter TGWs to `state in {"available"}` and optionally `OwnerId==ctx.account_id`.
- **opensearch M5** — `services/opensearch.py:76-79, 93-105`. Reads only data-tier `ClusterConfig`; Serverless collections, UltraWarm/cold, and dedicated-master are unpriced, and an empty-`ClusterConfig` domain (`InstanceType=''`, `InstanceCount=0`) still emits a graviton rec → empty advisory card. **Fix:** guard `InstanceCount<=0`/empty `InstanceType`; document Serverless/UltraWarm/master as out-of-scope or price via OCU/`ES:Managed-Storage`. *(Mirror the EKS 0-node guard.)*
- **quicksight M2** — `services/quicksight.py:27-31`. `user_optimization`/`capacity_optimization` declared but never populated; no author/reader pricing (live Reader $3.00, Reader Pro $20, authors ~$18-24). **Fix:** populate `user_optimization` from `list_users` + last-login activity, priced at edition-correct SKUs, evidence-gated as advisory until activity exists. *(Do with quicksight CRITICAL C1.)*
- **redshift M4** — `services/adapters/redshift.py:47-48`. In-code TODO: RA3 managed storage ($0.024/GB-Mo) is unpriced; the cost basis (and any derived RI/rightsizing saving) omits RMS, undercounting RA3 spend; paused RA3 clusters still bill RMS but aren't flagged. **Fix:** when `NodeType` starts with `ra3`, add `RMS = storage_GB * $0.024` (region-correct) to the cluster cost basis.

### Cluster H — Fast-mode & client-contract declaration desync

Shared anchor: `services/_base.py:24-25` defaults `requires_cloudwatch`/`reads_fast_mode` False; declare them and bail cheaply like `lambda_svc.py:61`/`rds.py:45-46`.

- **dms M3** — `services/adapters/dms.py:18`; `services/dms.py:54-72`. `reads_fast_mode=True` declared but `get_metric_statistics` runs unconditionally. **Fix:** gate the CPU read on `ctx.fast_mode` → $0 advisory when set, or drop the claim. *(Do with dms CRITICAL C2/C3.)*
- **dynamodb M3** — `services/adapters/dynamodb.py:50-59`. `requires_cloudwatch=True` but `reads_fast_mode` undeclared (inherits False), yet CW runs per qualifying table in `--fast`. **Fix:** declare `reads_fast_mode=True` and skip the metric blocks when `ctx.fast_mode`.
- **mediastore M2** — `services/mediastore.py:23-58`. `reads_fast_mode=True` but the helper never reads `ctx.fast_mode`; every ACTIVE container runs up to 5 CW calls. **Fix:** if `ctx.fast_mode`, skip the CW sweep / emit a $0 advisory.
- **mediastore M3** — `services/adapters/mediastore.py:12-22`. Reads four CW metrics but doesn't declare `requires_cloudwatch`. **Fix:** add `requires_cloudwatch: bool = True` (mirror s3.py/rds.py).
- **opensearch M1** — `services/adapters/opensearch.py:18-27`; `services/opensearch.py:123-139`. CW `get_metric_statistics` runs per domain with no fast-mode gate; `requires_cloudwatch`/`reads_fast_mode` undeclared, `required_clients()` omits cloudwatch. **Fix:** declare both flags, gate the read on `ctx.fast_mode`, add `cloudwatch` to `required_clients()`.
- **sagemaker M3** — `services/adapters/sagemaker.py:394-411, 133-134, 175-217, 220-303, 306-365`. `fast_mode` is passed only to `_check_idle_endpoints`; the other three checks run full list/describe regardless. **Fix:** pass `fast_mode` into all four and bail cheaply.
- **step_functions M2** — `services/step_functions.py:47-59`. Adapter gates dollarization on `if not ctx.fast_mode`, but the shim's per-machine CW read has no fast-mode check. **Fix:** gate the shim read on `ctx.fast_mode`.
- **step_functions M3** — `services/adapters/step_functions.py:51-71`. The shim reads `ExecutionsStarted` Sum over 7d for the gate; the adapter re-reads the SAME metric over 30d for the dead `eligible_for_migration` block → a second wasted CW call per Standard machine. **Fix:** have the shim pass through `MonthlyExecutions`/the 30-day Sum; consolidate to one read + one window. *(Tied to step_functions CRITICAL C1's dead gate.)*
- **transfer M3** — `services/adapters/transfer.py:12-21`. Declares neither `requires_cloudwatch` nor `reads_fast_mode` though the shim reads AWS/Transfer metrics gated on `ctx.fast_mode`. **Fix:** declare both (+ `cloudwatch` in `required_clients()`) if the CW note is kept, else remove the read. *(Do with transfer CRITICAL C1.)*

### Cluster I — Missing/weak `AuditBasis` (defensibility)

Shared remedy: attach a structured `AuditBasis` (rate / rate_source / region / metric-window / formula) per counted rec, mirroring `services/rds.py:585-600,651-661` and `lambda_svc.py`.

- **ami M3** — `services/ami.py:152,159-170`. Numeric `EstimatedMonthlySavings` (summed into the headline) carries no qualifier/`AuditBasis` though it's an upper bound (VolumeSize fallback ~2× the stored blocks). **Fix:** attach `AuditBasis` (rate/region/GB source = FullSnapshotSizeInBytes vs VolumeSize-fallback/formula); keep the "max estimate" qualifier wherever the number surfaces.
- **aurora M4** — `services/adapters/aurora.py:316-328,374-386,222-242`. None of the three checks attaches `AuditBasis` (instance recs carry only prose `reason`). **Fix:** add `AuditBasis` per counted rec (ACU rate + region + serverless window; I/O rate + window; instance price basis + CPU window).
- **bedrock M2** — `services/adapters/bedrock.py:151-164,200-212`. Both counted PT recs emit only free-text + a number (no rate/commitment-term/window/formula). **Fix:** attach `AuditBasis` (rate, rate_source, region_multiplier, commitment_term, metric_window_days, formula).
- **commitment_analysis M3** — `services/adapters/commitment_analysis.py:227-236, 341-350`. `sp_utilization` waste = `hourly×(1−rate)×730`; `ri_utilization` waste = `TotalAmortizedCost×(1−rate)` with NO `×730` — result-consistent today only because the 30-day tp makes `TotalAmortizedCost` ~monthly, but undocumented and fragile. **Fix:** attach `AuditBasis` (rate, window="30d", formula) and make both bases explicitly monthly.
- **mediastore M5** — `services/mediastore.py:95-109`. `unused_containers` rec has no `AuditBasis` for the borrowed S3-Standard rate, region, or `BucketSizeBytes` window. **Fix:** attach `AuditBasis` (rate `$/GB-mo S3 STANDARD (borrowed)`, rate_source, region, metric window, formula).
- **transfer M4** — `services/adapters/transfer.py:59-62`. The one counted check writes a bare float, no `AuditBasis`. **Fix:** attach `AuditBasis` (rate 0.30, hours 730, region, removable, multiplier). *(Do with transfer CRITICAL C1.)*

### Cluster J — Dead code & doc/code mismatch (no live $ impact; remove the hazard / reconcile the doc)

- **athena M3** — `services/adapters/athena.py:41-42`. Adapter reads `WorkGroup`/`ProcessedBytesTB` the shim never sets, so the CW query always targets the literal `"primary"`. **Fix:** set `rec['WorkGroup']=wg_name` when re-wiring the shim. *Covered under CRITICAL athena C1 — check off there.*
- **athena M4** — `services/adapters/CLAUDE.md` (athena.py Live-Pricing row). Row advertises a working `$5/TB` CloudWatch check; reality emits zero recs. **Fix:** state the shim currently emits nothing, or reconcile after athena C1. *Check off with athena C1.*
- **cloudfront M2** — `services/adapters/CLAUDE.md` (cloudfront Parse-rate row) + `services/adapters/cloudfront.py:1,13,27`. Doc/docstrings claim "Fixed $25/rec" / "Flat-rate" / "Traffic-based"; the adapter forces $0. **Fix:** update the row to "$0 honest advisory (needs CW BytesDownloaded + PriceClass)" and correct the docstrings.
- **cloudfront M3** — `services/cloudfront.py:31-32,86-132`; `services/adapters/cloudfront.py:56-59`. `low_traffic_distributions`/`origin_shield_unnecessary` buckets never appended; origin-shield CW math + `get_distribution_config` computed then discarded (`_ = (…)`); dead `"CloudFront Unused Distribution"` branch. Wastes 1 describe + up to 2 CW calls per distribution. **Fix:** remove the discarded computation and dead branches (or actually emit a quantified finding).
- **containers M2** — `services/adapters/containers.py:17-19`. `SPOT_SAVINGS_FACTOR=0.70` defined but referenced nowhere; `get_ecs_analysis` advertises "Save 70%" prose never converted to a rec. **Fix:** wire a defensible interruptible-gated Fargate-Spot delta as a `Counted=False` advisory, or remove the constant and the prose.
- **msk M2** — `reporter_phase_b.py:2377-2378` (+ 2349-2375). `_GENERIC_SOURCE_TYPES['enhanced_checks']='Metric Backed'` and no MSK override, so config-heuristic MSK cards are mislabeled metric-backed. **Fix:** add `SOURCE_TYPE_MAP[('msk','enhanced_checks')]='Audit Based'` (mirror the S3 fix at 2367).
- **workspaces M1** — `services/workspaces.py:130-133`. `if current_rank>=4 and ctx.pricing_engine: target_type="STANDARD"` gates the recommended target on engine truthiness (pricing uses the static table, not the engine) → same input yields a different downgrade target; `target_type="PERFORMANCE" if current_rank>2 else None` has dead `else None`. **Fix:** choose the target purely from rank/utilization, delete the unreachable branch.

---

## Execution order & dependencies

1. **SR-1** first (pricing engine) — unblocks correct SageMaker/OpenSearch instance pricing; fold the SageMaker `component` pin into **CRITICAL SR-1**.
2. **Fold MEDIUM items into the matching CRITICAL service commit** where they share a shim/file: dms (M1/M2/M3 with C1-C3), mediastore (M1-M5 with C1), lightsail (M2/M3 with C1), opensearch (M2/M3/M5 with C2), redshift (M1/M2/M3 with C1; M4 after SR), elasticache (M1/M2/M4 with C1/C2), batch (M1/M3 with C1), athena (H3/M1/M3/M4 with C1), step_functions (M1/M2/M3 with C1), quicksight (M1/M2 with C1), aurora (M1 with C1), api_gateway (M2 with C1/SR-2).
3. **Cluster A (silent failures)** next — highest-leverage correctness/visibility, mostly mechanical `record_aws_error` swaps; one commit per service.
4. **Clusters B, C, D** (fabrication / reduction-factor / wrong-rate) — these change counted dollars; validate every rate live and expect golden/reporter snapshot shifts.
5. **Cluster E (dedup)** — independent per service; cap any single resource's counted saving at its monthly cost.
6. **Cluster F (counted==rendered)** — will shift reporter snapshots; refresh intentionally.
7. **Clusters G, H, I, J** (coverage / contract / AuditBasis / dead-code) last — low dollar risk; G and I may add advisory cards, H/J are mostly declarations and reconciliations.

## Definition of done

- All 109 MEDIUM findings fixed, honestly demoted to `$0` advisory (`Counted=False`), or explicitly dropped after confirming the asserted defect is absent in current code (note which, since these were unverified).
- Every counted dollar validated against the live AWS Pricing API and carrying an `AuditBasis`.
- `counted == rendered` for every affected adapter (no string-vs-number desync; every `$0`/advisory rec is `Counted=False` and the tab header reconciles).
- New/updated `tests/test_<svc>_audit_fixes.py` per service; full suite green.
- Regression gate green: `pytest tests/test_regression_snapshot.py tests/test_reporter_snapshots.py` (golden/reporter snapshots refreshed **intentionally** for SR-1 and the Cluster D/F dollar changes, with the diff reviewed and called out).
- `services/adapters/CLAUDE.md` rows reconciled where a fix changes pricing/dedup/coverage behavior (athena, cloudfront, msk source-type).
- MEDIUM items folded into a CRITICAL service commit are checked off in that commit; stage only changed files; one cluster (or one service's combined fix) per commit.

## PROMPT (end)