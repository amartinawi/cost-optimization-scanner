# Independent Verification — RUN-FINDINGS-2026-08-09 (`audit-services-deep`)

- **Verification date:** 2026-08-09
- **Verified against:** HEAD `935d884` (post-merge of `fix/aurora-grouped-findings` + `fix/commitment-jarir-audit`); working tree contains no `.py` changes.
- **Method:** (Pass 1) every one of the 35 NEW findings re-verified against current code, with independent re-derivation of the three counted-dollar MEDIUM candidates against live AWS Pricing API SKUs and AWS API documentation; (Pass 2) stratified sample of 25 CONFIRMED-already-fixed claims (3+ per cohort, mixed line ranges/severities) checked by two independent read-only agents; (Pass 3) from-scratch A1–F5 lessons sweeps of three adapters (rds — high-dollar/thinnest coverage; s3 — quiet C5 cohort; elasticache — random pick) by three independent read-only agents blinded to the report.
- **This verification is read-only** apart from this document. No `.py`, no prompts, no `_LIVE_AUDIT_LESSONS.md`, no git state touched.

---

## Top-line scorecard

| Metric | Result |
|---|---|
| NEW findings re-verified (all 35) | **30 CONFIRMED-ACCURATE** |
| REFUTED | **2** (eks_cost fallback constant; apprunner pagination) |
| DOWNGRADED | **2** (dynamodb fast-mode severity; redshift `required_clients` mechanism) |
| Escalation recommended | **1** (athena unconditional 0.75 factor — MED → HIGH candidate) |
| Weak citations | **2** (the two REFUTED items — one no-URL cite, one misread pricing page) |
| Merge-shifted citations (substance intact after re-anchor) | 4 (all in `services/adapters/commitment_analysis.py`) |
| CONFIRMED-already-fixed sample (Pass 2, 25 items) | **25/25 VALID — no rubber-stamping** |
| False negatives discovered (Pass 3, 3 adapters) | **~20 missed findings: 1 CRITICAL, 5 HIGH, 7 MEDIUM, rest LOW** — all three sampled adapters (each scored NEW: 0 by the report) produced material misses |
| Honesty invariant (CONFIRMED-dominant on STALE-banner services) | **HELD** as stated, but see the false-negative caveat below |

---

## Pass 1 — Per-finding verdicts on the 35 NEW findings

Verdicts: **CONF** = CONFIRMED-ACCURATE (defect present as described, cite and class check out), **REF** = REFUTED, **DG** = DOWNGRADED, with notes. Line numbers are CURRENT code (re-anchored where the mid-audit merge shifted them).

| # | service | finding | verdict | evidence |
|---|---|---|---|---|
| 1 | ec2 | outer `except` generic `ctx.warn` (E1) | CONF | `services/ec2.py:834-835` and `:1223-1224` exactly as cited; burstable handler at `:818-827` classifies — asymmetry real |
| 2 | ebs | `compute_ebs_checks` outer `except` generic (E1) | CONF | `services/ebs.py:689-690`; `get_unattached_volumes` classifies at `:322-329` — asymmetry real |
| 3 | eks_cost | Extended-Support fallback stale ($0.50 vs "$0.60") | **REF** | Live Pricing API (publication 2026-08-04): SKU `M7977BSVFGDUJZ67`, `USE1-AmazonEKS-Hours:extendedSupport` = **$0.50/hr**. The $0.60 on the EKS pricing page is the TOTAL extended-support cluster price ($0.10 base + $0.50 surcharge). The code models the surcharge (comment `core/pricing_engine.py:357-359`) and matches the billed SKU exactly. Applying the proposed fix would have INTRODUCED a ~$73/mo/cluster overstatement. |
| 4 | eks_cost | `extended_support_pending` non-zero `AdvisoryEstimate` | CONF | `services/adapters/eks.py:364-366`: `monthly_savings: 0.0`, `Counted: False`, `AdvisoryEstimate` non-zero — the sanctioned B1-iii shape, as the finding itself concedes. Accurate but a non-defect documentation note. |
| 5 | containers | `SPOT_SAVINGS_FACTOR = 0.70` dead code | CONF | Declared `services/adapters/containers.py:20`; zero references in containers.py (repo grep). Note: `services/adapters/eks.py:53` declares its own same-named constant which IS used (`:581`) — the containers one is the dead copy. |
| 6 | apprunner | `list_services` un-paginated drops services | **REF** | Code fact true (`services/apprunner.py:100`, no paginator) but the claimed impact does not exist: the App Runner `ListServices` API doc states "If you don't specify `MaxResults`, the request retrieves **all available results in a single response**." No service is dropped. The finding cited no AWS URL (weak cite); the authoritative source refutes it. Adding a paginator remains harmless hygiene only. |
| 7 | aurora | 3 CW helpers swallow errors (E1) | CONF | `services/adapters/aurora.py:119-121`, `:147-149`, `:171-173` — `except Exception: pass` / `return None`, no `ctx` in signatures; enum helpers correctly classify (`:55`, `:73`). Line-accurate. |
| 8 | dynamodb | `reads_fast_mode` not declared; CW ungated | **DG** | Code claim accurate: no `reads_fast_mode` on `DynamoDbModule` (`services/adapters/dynamodb.py:57-69`); shim CW reads ungated (`services/dynamodb.py:142-159` etc.); rds/aurora declare it (`rds.py:47`, `aurora.py:622`). But this is scan-performance, not cost-fidelity — no dollar can be wrong in either direction. MEDIUM is inconsistent with the LOW assigned to equal-impact hygiene items; recommend LOW. |
| 9 | dynamodb | on-demand `EstimatedMonthlyCost` unit semantics | CONF | Formula at `services/dynamodb.py:417-419` as cited (CW `Average` × per-request rate × 730 — semantics genuinely questionable); flows only into the demoted `table_analysis` advisory. Advisory-only, LOW appropriate. |
| 10 | redshift | serverless block bare `except: pass` (E1) | CONF | `services/redshift.py:122-123` wraps the whole `redshift-serverless` block; outer `:125-126` warns only for provisioned. Line-accurate. Severity ambiguous in the report (MED per C3 notepad, LOW in the E1 cluster) — see report-integrity notes. |
| 11 | redshift | `required_clients()` omits `redshift-serverless` | **DG** | Omission real (`services/adapters/redshift.py:40-42`). Mechanism claimed ("orchestrator only pre-instantiates declared clients; undeclared client may be unavailable") is WRONG: `ctx.client()` is a lazy caching factory (`core/scan_context.py:75-77` → `ClientRegistry`), and `required_clients` has no consumer outside the Protocol definition (`core/contracts.py:177`). The undeclared client works fine at runtime. Residual value: declaration hygiene only → LOW/cosmetic. |
| 12 | opensearch | per-domain CW/describe failures logger-only (E1) | CONF | `services/opensearch.py:229-231`, `:233-234` logger.warning; outer `:236-237` `ctx.warn`. Line-accurate; elasticache sibling was fixed. |
| 13 | msk | `kafka:ListClusters` generic warn + inner bare pass (E1) | CONF | `services/msk.py:91-92` (`except Exception: pass`, moot v2 block), `:94-95` outer generic warn. Line-accurate. |
| 14 | msk | `enhanced_checks` mislabeled "Metric Backed" | CONF | `reporter_phase_b.py:2761` SOURCE_TYPE_MAP has no msk entry (repo grep); `:2794-2795` generic maps `enhanced_checks → "Metric Backed"`; msk declares only `("kafka",)`, no CloudWatch (`services/adapters/msk.py:107-109`). Cosmetic, F4-class. |
| 15 | transfer | outer `except` bare `ctx.warn` (E1) | CONF | `services/transfer_svc.py:124-125` verbatim as cited. Severity-consistency note: identical shape to ec2/ebs items rated LOW. |
| 16 | transfer | inner CW `except` swallows with no ctx record (E1) | CONF | `services/transfer_svc.py:98-103` verbatim; correctly notes the empty-datapoints path should stay silent. Same severity-consistency note. |
| 17 | lambda | PC metric dimensioned by `FunctionName` only | CONF | `services/lambda_svc.py:100-108` (Dimensions = FunctionName only); shim comment `:230-232` asserts "the metric is keyed by FunctionName". AWS doc (monitoring-metrics-types): `ProvisionedConcurrencyUtilization` is defined "**for a version or alias**" — PC only attaches to versions/aliases, so if anything the report UNDERSTATES the blast radius ("alias/version configs MAY degrade" → effectively all PC configs are at risk). A readable low utilization produces a counted saving (`services/adapters/lambda_svc.py` PC path), so real counted dollars are suppressed. MEDIUM appropriate. |
| 18 | lambda | ARM runtime allowlist drift | CONF | `services/lambda_svc.py:47-65` tuple exactly as described; advisory-only. LOW appropriate. |
| 19 | athena | adapter CW failure logger-only (E1) | CONF | `services/adapters/athena.py:64-66` verbatim (`logger.warning`, `monthly_tb = 0` → $0 advisory); shim `list_work_groups` DOES use `record_aws_error` (`services/athena.py:53-54`). Line-accurate. |
| 20 | athena | provisioned-capacity misprice + unconditional 0.75 | CONF + **ESCALATE→HIGH recommended** | Verified: `services/adapters/athena.py:71` counts `monthly_tb × $5 × multiplier × 0.75` into the tab total (`:82`, summed and Counted); the shim reads ONLY `Name`/`State` from `list_work_groups` — no `get_work_group`, no engine/capacity/format detection anywhere. Both SKU citations verified live: `USE1-CodeExecutionInDPUHours` = $0.35/DPU-Hr (Athena Spark), `USE1-ReservedCapacityInDPUHours` = $0.30/DPU-Hr. Escalation rationale: EVERY counted athena dollar is a flat 75% of measured scan spend with zero evidence the workgroup is not already columnar/partitioned — the exact C9 shape ("a flat percentage is a fabricated dollar... each is a fabricated dollar until it is two live prices") that was rated CRITICAL for dms 0.35 and HIGH for elasticache 0.30 / opensearch 0.25. The measured-`ProcessedBytes` gate makes the BASE spend real, but the 0.75 realizable fraction is fabricated per-workgroup. MEDIUM understates it by this codebase's own precedent. |
| 21 | network | VPC interface-endpoint nonprod ∩ duplicate double-count | CONF | Fully re-derived. Nonprod loop `services/vpc_endpoints.py:109-123` and duplicate loop `:130-148` each attach `vpc_ep_monthly × az_count` numerics; no shared `VpcEndpointId` set; an endpoint that is nonprod-tagged AND 3rd+ of its `vpc:service` is counted twice. Both categories carry non-zero `EstimatedMonthlySavings`, survive `mark_zero_savings_advisory`, and enter the counted sum at `services/adapters/network.py:210-211`. Real counted double-$$; MEDIUM (bounded intersection) is right. A3/A4 class match. |
| 22 | network_cost | `TGW_ATTACHMENT_COST_PER_GB` unit-misleading | CONF | `services/adapters/network_cost.py:49` ($0.05 is the per-attachment-HOUR rate; name says per-GB); `$0.07/GB` conflation displayed at `:443,451,455-456,473`. All affected recs `monthly_savings: 0.0` advisory. Cosmetic, LOW. |
| 23 | network_cost | peering/TGW describes un-paginated | CONF | `:403`, `:410` single calls; feed advisory-only counts; errors classified (`:407`, `:415`). Moot/LOW as stated. (Listed in the report's LOW cluster but absent from per-service tallies — bookkeeping note below.) |
| 24 | api_gateway | REST-only coverage, intentional | CONF | Module docstring `services/api_gateway.py:1-21` documents the deferral with 2025-11-20 SKU validation; `:75` paginates `get_rest_apis` only. Accurate — though as a self-described intentional documented limitation with "no fix recommended", it pads the NEW count. |
| 25 | commitment_analysis | `_account_coverage_ratio` silent swallow (E1) | CONF (shifted) | Now `services/adapters/commitment_analysis.py:667-691`; `except Exception: return DEFAULT_COVERAGE_RATIO` at `:690-691`, no `ctx` record. Cited `:648-672` was pre-merge; substance intact. |
| 26 | commitment_analysis | RI expiry never detected (SP-only) | CONF (shifted) | Comment `:451-454` documents SP-only; `_check_expiring` at `:455` reads SP utilization details only. |
| 27 | commitment_analysis | `ri_coverage_gaps` always empty | CONF (shifted) | Comment `:416-419`; `recs=[]` never appended, returned `:449`. Documented intentional. |
| 28 | commitment_analysis | Fargate legs ECS-only, EKS-on-Fargate missed | CONF (shifted) | Comment `:595-598` documents the exclusion; `SERVICE = "Amazon Elastic Container Service"` filter at `:609`. |
| 29 | sagemaker | non-deterministic pricing filter | CONF | `core/pricing_engine.py:1550-1556` bare `instanceType + location`; the generic helper's own docstring (`:1558-1577`) warns this exact shape "returns a non-deterministic, frequently wrong dimension" and pins for Redshift/ElastiCache. Line-accurate. (Arguably touches counted idle-endpoint dollars — LOW is at the light end but defensible given SageMaker instance SKUs share rates across usage types.) |
| 30 | sagemaker | multi-variant / `InstanceCount` under-pricing | CONF | `services/adapters/sagemaker.py:165-167` prices `variants[0].InstanceType` × 1 only. Under-count direction. LOW appropriate. |
| 31 | bedrock | blended flat token rate on PT-breakeven | CONF | `services/adapters/bedrock.py:316` (`(input+output) × 0.000_003`) feeding counted `monthly_savings` `:331`, summed with no `Counted` filter at `:487`. Rate math verified: 3e-6/token = Claude 3 Sonnet INPUT rate exactly ($3/M); output is $15/M (5×), Haiku input $0.25/M (12× lower) — savings overstated for output-heavy Sonnet PTs, understated/gate-flipped for Haiku. Bounded to the od<pt niche as stated. MEDIUM right. |
| 32 | bedrock | Claude PT rates unverifiable (no public SKU) | CONF | `PT_HOURLY_PRICE` `:20-26`; unknown-model gate at `:301-306` prevents silent miscount. Absence-of-SKU claims are inherently hard to prove but consistent with Bedrock PT pricing not being in the retail Pricing API. LOW. |
| 33 | bedrock | KB recs carry no `Counted` flag | CONF | KB rec dict `:378-391` (no `Counted` key, `monthly_savings: 0.0`); `total_savings` sums `monthly_savings` at `:487` ($ unaffected); `total_recommendations = len(all_recs)` at `:494` (count includes KB advisories). Cites exact to the line. LOW/D4. |
| 34 | bedrock | CW dimension `ModelId` for a PT | CONF | Dimensions at `:133`, `:160`, `:175` (report said 159 — off by one, trivial). Base `model_id` used; `definitive` flag mitigation confirmed (`:110-122`). LOW. |
| 35 | monitoring | backup/route53 sub-shims generic `ctx.warn` (E1) | CONF | `services/backup.py:111-122` (3 excepts, all `ctx.warn`); `services/route53.py:159-160,178-179,237-238` — no `record_aws_error` in either file. Line-accurate. Severity-consistency note as with #15/#16. |

*(#23 is verified as a bonus: it appears in the report's LOW-25 cluster but not in any per-service NEW tally. The athena `list_work_groups` pagination sub-note was also verified: single un-paginated call at `services/athena.py:35`; the Athena `ListWorkGroups` API does paginate via NextToken/MaxResults≤50 — unlike App Runner, no "returns all" contract — so that sub-note stands.)*

### The three counted-$ MEDIUM candidates — independent re-derivation summary

1. **network VPC endpoints (#21): REAL.** Full trace from tag-match to counted sum; the overlap double-counts `vpc_ep_monthly × az_count` (≈$7.30/AZ/mo us-east-1) per endpoint that is both nonprod-tagged and 3rd+ of its service. Correctly the report's top counted-$ item. Severity MEDIUM: correct (bounded intersection; CoH/CO untouched).
2. **bedrock blended token rate (#31): REAL.** Constant equals Claude 3 Sonnet input rate; ignores the 5× output rate and 12× cheaper Haiku input. Fires only when a PT has token traffic AND od-estimate < PT cost AND savings > $1. Severity MEDIUM: correct.
3. **athena provisioned + 0.75 (#20): REAL and UNDERSTATED.** Both pricing SKUs verified live. The unconditional 0.75 on every counted athena dollar is the codebase's own C9 cardinal pattern; recommend HIGH.

---

## Pass 2 — CONFIRMED-already-fixed sample (25 items, stratified 3+ per cohort)

**Result: 25/25 VALID. No rubber-stamping detected.** Two independent read-only agents each verified their half against current code (not comments — actual gating/demotion logic).

Sampled and confirmed (service — claimed fix — verdict):

- **C1:** ec2 C6 five-source commitment demotion (`services/adapters/ec2.py:286-319`, `_split()` on all 5 sources) — VALID · ami `counted_snapshot_ids` cross-AMI dedup + all-shared advisory — VALID · lightsail unknown-bundle → None → $0 advisory; `medium_2_0` fallback absent (grep; comment-only) — VALID
- **C2:** eks_cost surcharge gated on `version_status == "EXTENDED_SUPPORT"` (`:325`) — VALID · containers arch/os-aware Fargate pricing, Windows leg on `startswith("win")` — VALID · apprunner 160-hr assumption gone; `RIGHTSIZING_SAVINGS_RATE` zero grep hits — VALID
- **C3:** rds `reconcile_snapshot_savings` string+numeric lockstep from one `capped_sv` — VALID · dynamodb `_sum_gsi_throughput` + per-GSI CW dimension — VALID · redshift CoH bucket consumed, `coh_keys` spans all CoH recs, `demote_coh_by_commitment` — VALID
- **C4:** elasticache CoH consumption (`is_renderable_coh_rec`/`coh_key`/orchestrator `:114`) — VALID · opensearch `*-OpenSearchExtendedSupport` CE usage type, 7d×30/7, additive SourceBlock — VALID · msk pricing filter `computeFamily+productFamily+RunBroker`, `MSK_BROKER_OVER_EC2 = 2.19` — VALID
- **C5:** s3 `_assess_bucket_coldness` zero-GET evidence gate — VALID (but see Pass 3 S3-1 for a fail-open hole *inside* this same gate) · file_systems EFS IA `0.025` at `core/pricing_engine.py:263` — VALID · dms `_DMS_TERMINATION_FACTOR = 1.0` + `allow_fallback=False` both legs — VALID · glue `ASSUMED_MONTHLY_DPU_HOURS` absent repo-wide — VALID
- **C6:** lambda PC constants exact (0.0000041667/0.0000033334), multiplier once — VALID · step_functions all-advisory + `_FLAT_SAVINGS_SERVICES` absent as a live symbol — VALID (slightly shifted lines) · athena `/1e12` TB divisor — VALID
- **C7:** network per-NAT-id CoH exclusion (`coh_savings > 0` filter; `if nat_id in exclude: continue`) — VALID · network_cost full-token classifier, cross-AZ before inter-region — VALID · mediastore `activity_read_failed` abstain + `datapoints_seen > 0` gate — VALID
- **C8:** commitment_analysis advisory boundary (`Counted=False` on sp_cov/ri_cov/cost_hub; purchase-card builders hardcode `Counted: False`) — VALID · sagemaker `idle_endpoint_names` fed into consolidation — VALID · workspaces CW-gated AutoStop, `_AUTOSTOP_SAVINGS_FACTOR` zero grep hits — VALID

The three claimed constant-removals (glue 160-hr, `_FLAT_SAVINGS_SERVICES`, `_AUTOSTOP_SAVINGS_FACTOR`) were grep-verified genuinely absent as live symbols. A few cites were a few lines stale (post-merge drift) — none changed a verdict. **The CONFIRMED backbone of the report is trustworthy.**

---

## Pass 3 — Independent false-negative sweeps (rds, s3, elasticache)

Three adapters were re-swept from scratch against `_LIVE_AUDIT_LESSONS.md` A1–F5 by independent read-only agents blinded to the report (they did not read the report or the notepads). Adapter selection: **rds** (high-dollar, thinnest coverage in the report — 5 CONFIRMED / 0 NEW), **s3** (quiet C5 cohort — 0 NEW), **elasticache** (random pick — 0 NEW). **All three produced material findings the report missed.** Items marked ✓ were independently re-verified by the consolidating verifier (code read and/or live Pricing-API probe); the rest carry the sweep agent's own citations, which follow the same evidence standard.

### elasticache (report scored NEW: 0) — 6 missed findings

- **EC-1 · CRITICAL · Graviton lever counts the FULL node price when the Graviton target resolves to $0.** ✓ `services/adapters/elasticache.py:200-207`: the gate is `if graviton_type and delta > 0:` with **no `graviton_node > 0` guard** — the sibling downsize branch HAS it (`:251` `target_node > 0`). `get_instance_monthly_price` falls back to `0.0` on a missing/unreadable SKU, so `delta` becomes the entire current node cost, counted. Deterministic trigger: `graviton_equivalent` maps sizes that don't exist in Graviton families (`cache.m5.24xlarge → cache.m6g.24xlarge`, `cache.r5.24xlarge → cache.r6g.24xlarge`, `cache.m4.10xlarge → cache.m6g.10xlarge` — none published in the Price List) → phantom counted savings up to **~$7,568/mo/node** (r5.24xlarge Redis us-east-1). Non-deterministic trigger: any throttle/AccessDenied on the second pricing lookup — a C8 shape where losing evidence RAISES the counted total. Classes C9+C8. Prose fix: add `graviton_node > 0` to the gate AND probe the hypothetical target with the strict no-fallback lookup pattern already built for DMS (`allow_fallback=False`, own cache namespace); add the missing Graviton twin of `test_underutilized_lever_advisory_when_target_unpriceable`.
- **EC-2 · HIGH (missed live surcharge) · Redis OSS Extended Support is billing today; the shim's comment asserts it cannot exist.** ✓ `services/elasticache.py:179-180` comment verbatim: "Redis 7 upgrade is free; **engine cost is identical**." False since 2026-02-01: ElastiCache auto-enrolled Redis OSS v4/v5 into Extended Support (v6 follows 2027-02-01), billed as a distinct usage type stacked on base NodeUsage. Live-verified SKU ✓: `USE1-ExtendedSupportYr1_Yr2-NodeUsage:cache.r6g.large` = **$0.165/hr** (SKU `AHKSH4UM9QD2WMH5`, effective 2026-07-01) — +$120.45/mo/node, ~80% of the $0.206 base rate; Yr3 doubles it. A 6-node Redis 5.x replication group on r6g.large bills ~$722.70/mo of removable surcharge; the scanner reports $0 while already reading `EngineVersion` and discarding it. This is the converse-C7 shape the lessons file warns about ("missed one that did [bill]") — same failure as the bnc OpenSearch case. Sources: docs.aws.amazon.com/AmazonElastiCache/latest/dg/extended-support-versions.html and …/extended-support-charges.html. Prose fix: measure from the billed `*ExtendedSupport*NodeUsage*` CE usage type (trailing 7d × 30/7, per C7), fail closed to a $0 advisory naming the EOL date; also note the Valkey lever currently understates its own value on surcharged clusters (migration removes the surcharge too).
- **EC-3 · HIGH (conditional) · CoH↔heuristic dedup cannot match replication-group-keyed CoH recs.** `services/adapters/elasticache.py:300` suppresses by raw `CacheClusterId` equality against normalized CoH keys, but the shim never captures `ReplicationGroupId`; Redis members are named `<rg>-001` etc., so a CoH rec ARN'd at replication-group granularity matches nothing → CoH counted dollar + heuristic Valkey/Graviton dollar can both count the same nodes (A1/A3). Conditional on CoH's ARN granularity for `ElastiCacheCluster` (unconfirmed — the sweep says so plainly); the code-level gap (no RG key captured; a successful match suppresses only 1 of N members) is unconditional. Prose fix: capture `ReplicationGroupId` in the shim and suppress by both keys; settle the ARN-granularity question on a live account with a CoH ElastiCache rec.
- **EC-4 · MEDIUM · The C10 memory gate fails OPEN on a failed Evictions read.** ✓ `services/elasticache.py:67-68` returns `(peak_pct, None)` on exception; the gate at `:236-240` evaluates `(evictions or 0) == 0` → `None` scores as zero evictions → downsize counted on unverified evidence, and the card renders "None evictions". Classes C8/C10. Prose fix: `evictions is not None and evictions == 0`.
- **EC-5 · MEDIUM · E1 gaps:** `services/elasticache.py:53,67` (`_memory_signals` bare excepts — a denied `DatabaseMemoryUsagePercentage` read silently withholds every downsize dollar with no `permission_issue`) and `:271-272` (outer `describe_cache_clusters` handler is generic `ctx.warn`). Same class as the report's own E1 cluster — missed on this adapter.
- **EC-6 · LOW · `_NODE_SIZE_ORDER` assumes contiguous sizes.** `services/adapters/elasticache.py:70-112`: m5/r5 skip 8xlarge/16xlarge, so `cache.m5.12xlarge` targets nonexistent `m5.8xlarge` → $0 advisory where a real 12xl→4xl downsize exists (safe direction, material dollars withheld). Adjacent: `SMALLEST_SIZES` lists nonexistent `cache.*.nano`; `GRAVITON_FAMILIES` omits m8g/r8g. Prose fix: derive per-family ladders from the Pricing API `instanceType` set.

The sweep also *cleared* (with cites): Valkey 0.20 is exactly the real two-price delta (live-verified, NOT a C9 fabrication); `total_recommendations` inflation is neutralized by `ScanResultBuilder._counted_recommendations`; the elasticache CoH bucket is fully wired (supersedes the old "orphaned buckets" note); B1/B2/C6/D1/D2 hold.

### rds (report scored NEW: 0) — 6 missed findings

- **RDS-1 · HIGH · The C6 CE-headroom ceiling is spent TWICE per instance type (and a third time by aurora).** ✓ `services/adapters/rds.py:148` (`demote_coh_by_commitment`) and `:158-164` (`demote_covered_in_place`) each allocate an independent greedy `budgets: dict = {}` (`services/commitment_coverage.py:352` and `:458` ✓) over the same frozen `realizable_ceiling` — the second call starts from the FULL ceiling as if the first spent nothing. Worse: `_CE_SERVICE_DIM` maps BOTH `"rds"` and `"aurora"` to "Amazon Relational Database Service" (✓ `:669-676`), writing the same billed pool under both keys, which `services/adapters/aurora.py:727` spends a third time. Scenario: $500/mo uncovered on-demand for `r7i.4xlarge`; a $400 CoH rec and a $450 enhanced rec on two different DBs both stay counted → $850 counted vs $500 real headroom. Violates the C6 sweep invariant verbatim ("counted rightsizing savings ≤ CE uncovered-on-demand for that type"). Manifests on the NORMAL path (CE resolved); empty-CE falls back to demote-all (safe). Prose fix: thread one shared budget dict through both calls per adapter, and partition (or share) the rds/aurora pool. (Connects to memory'd deep-dive follow-up M6: CoH typed-matching unverified.)
- **RDS-2 · HIGH · Snapshot reconciliation caps at 100% of the region's ENTIRE billed backup pool (C11, no share factor).** `services/rds_logic.py:158-162` caps the flagged snapshots' provisioned-size upper bound at the whole `RDS:ChargedBackupUsage`/`Aurora:BackupUsage` pool (built in `services/advisor.py:149-159`) — which includes automated backups and all other snapshots. When the bound exceeds the pool, factor binds at 100% and the tab counts the entire regional backup bill against a few old snapshots. **The lessons file itself names this exact function as having "the same shape" (C11, final sentence)** — the audit applied the C11 sweep and still scored rds NEW: 0. Prose fix per C11: ceiling = billed × (flagged footprint / total footprint), demote when the share is unmeasurable.
- **RDS-3 · MEDIUM · B1-iii render premise unmet + counted-category under-render.** `demote_covered_in_place` called without `zero_keys` (`services/adapters/rds.py:158-164`) leaves demoted enhanced recs with full dollar strings, and `CommitmentCoverageNote`/`AdvisoryEstimate` appear NOWHERE in `reporter_phase_b.py`/`html_report_generator.py` (grep) — so a demoted Multi-AZ card renders "Estimated Savings: $120.00/month" with no advisory marker while contributing $0 (the B1-iii sanction presumes the card says "covered by your SP/RI"; it doesn't). Same parse (`reporter_phase_b.py:686`) throws on "…/month with …" strings → a counted N-database category renders only `recs[0]`'s dollar. CoH mixed groups: header excludes demoted (`:2683`) but per-row cells print full demoted dollars (`:2724`).
- **RDS-4 · MEDIUM · The engine-scoped RI gate NEVER fires (key-case bug).** ✓ `services/adapters/rds.py:163` reads `r.get("Engine")` (capital E); every emitted rec writes lowercase `"engine"` (✓ `services/rds.py:463,509,619,682,…`) → `engine_of` always returns `""` → the `(family, engine)` branch in `covers_rds` is dead and the adapter comment at `:145-147` asserting it works is false. Direction: over-demotion (an `aurora-mysql` RI demotes a same-family `sqlserver-se` instance) — no phantom, but silently discards realizable savings on mixed-engine accounts. One-character-class fix.
- **RDS-5 · LOW · Pricing failure silently deletes the lever (E1).** `services/rds.py:101-103` returns 0.0 on any exception (`logger.debug` only) → the rec never enters candidates → neither counted nor rendered, no `ctx.warn`.
- **RDS-6 · LOW (latent, not reachable today).** `rec_gross`'s digit-scrape fallback (`services/commitment_coverage.py:385-388`) parses the Backup-Retention advisory string to a nonsense 1000.095; currently blocked by two gates — flagged so neither gate is relaxed unknowingly.

Cleared with cites: C8 early-return FIXED (`backup_actuals or {}` falls through to per-group demotion), A1/A3/A4, B2 lockstep, C1, C9 (Multi-AZ is a true two-leg delta; the 0.65 non-prod factor is duty-cycle arithmetic, judged defensible), C10, D-series (adapter `total_recs` drift is dead code — ResultBuilder recounts), E2, F1/F5.

### s3 (report scored NEW: 0) — 2 HIGH, 3 MEDIUM, 6 LOW missed

- **S3-1 · HIGH · The coldness gate treats "no CloudWatch data" as proof of zero access (fails OPEN).** ✓ `services/s3.py:906`: `return "cold" if total_get_requests == 0 else "warm"` — config-absence, unreachable-CW, and AccessDenied all correctly return "unknown", but an EMPTY `Datapoints` list scores as a measured zero. A metrics config enabled 3 days ago on a weekly-read 8 TB bucket ⇒ counted ~$86/mo Standard→IA with a `PricingBasis` asserting "0 GET requests over 30d" that the data does not support. AWS documents S3 request metrics as best-effort ("might not be delivered at all" — metrics-dimensions user guide). This gate protects 100% of the S3 counted total; classes C8/C10. Prose fix: corroborate with `AllRequests` for the same filter/window; return "unknown" when it too is empty.
- **S3-2 · HIGH · Standard→IA delta ignores the 128 KB minimum object size and transition rules; sign-wrong for small-object buckets.** `services/s3.py:1191-1195` prices `standard_gb × (std − ia)` with no object-count input anywhere in the module. AWS: objects <128 KB created after Sept 2024 cannot lifecycle-transition at all; Standard-IA bills a 128 KB minimum per object; 30-day minimum + $0.01/1k transition requests. A 500 GB / 20M-object cold bucket counts +$5.25/mo saving where reality is $0 (no transition) or a **+$19/mo cost increase** plus ~$200 one-time. Class C10 ("a recommendation you would have to revert is not a saving"). Prose fix: gate on `SizeBytes/NumberOfObjects ≥ 128 KB` via the free `NumberOfObjects` CW metric (client already built); demote when unreadable.
- **S3-3 · MEDIUM · Standard rate pinned to the first volume tier** (`core/pricing_engine.py:2102-2109`, `beginRange == "0"` → $0.023): bytes leaving Standard come off the account's MARGINAL tier ($0.022 >50 TB, $0.021 >500 TB, live SKU `WP9ANXZGBYYSGJEA`), overstating the delta up to ~19% for large accounts (~$1k/mo at 500 TB flagged).
- **S3-4 · MEDIUM · `IntelligentTieringStorage` is not a valid CloudWatch StorageType** (`services/s3.py:766-773`) — the real filters are `IntelligentTiering{FA,IA,AA,AIA,DAA}Storage`; a bucket fully in INT reports SizeGB 0, $0 cost, drops from top-buckets and is suppressed by the 1-GB render floor. Under-count direction; report-fidelity material. (`GlacierInstantRetrievalStorage`, `ExpressOneZoneStorage` also unmapped.)
- **S3-5 · MEDIUM · Dead-CW-region `continue` drops the whole bucket** (`services/s3.py:1038-1044`) — discards the just-written `MetricsSkipped` bucket_info (dead store) and skips the CW-independent lifecycle/website checks; the parallel path at `:1104-1107` deliberately does NOT continue, proving the intent. E1/D1-inverse.
- **LOW (6):** GLACIER key-drift prices Glacier bytes as Standard on the `pricing_engine=None` path only (`:797` vs `S3_STORAGE_COSTS` keys); `Datapoints[-1]` assumes chronological order (`:1068`); `list_bucket_metrics_configurations` unpaginated (`:848`, fail-closed); fast-mode sample size feeds the 1-GB render floor; counted bucket dicts serialized twice into one source block via `extras` (latent A3 hazard, nothing sums them today); module-global dead-region caches never reset between in-process scans (`:52-57`).

Cleared with cites: A-class (single dollar-bearing source + `_DEDICATED_CATEGORIES`), B1/B2/B3, C1 (multiplier `del`'d), C4 (factors gone), C5, D1/D4, E2 (s3 CoH removal is deliberate and honest — supersedes the old orphan note), both `GetBucketWebsite` call sites classified, pagination of `list_buckets` correct.

---

## Report-integrity observations (bookkeeping, not code)

1. **Severity arithmetic is internally inconsistent.** Per-service sections label 13 findings MEDIUM (aurora, dynamodb fast-mode, redshift ×2, transfer ×2, lambda PC, athena ×2, network, commitment_analysis, bedrock, monitoring) but the headline claims 10 MED / 25 LOW. The reconciliation notes contradict each other: line ~572 says redshift's two would "bring the MEDIUM count to 12" (i.e., they are NOT counted as MEDIUM), while line ~592 says they are "counted once each as MEDIUM in the headline (10 MEDIUM total)" — arithmetically impossible alongside the 10-item MEDIUM list that excludes them. The lambda PC finding is MEDIUM in its per-service section yet sits in the LOW-25 cluster (item 18) with a "(MEDIUM per C6 notepad)" aside.
2. **The LOW-25 cluster does not map 1:1 to per-service NEW entries.** It contains a duplicate (dynamodb units listed as both #13 and #17, "counted once"), two items absent from per-service tallies (athena pagination #14; network_cost pagination #16), two items the per-service sections call MEDIUM (redshift #7-#8), and three unnumbered commitment items beyond the 25 numbering. The unique-finding total (35) survives; the severity split as published does not reconcile cleanly.
3. **The consolidation's "all three citations line-accurate" spot-check claim** was already stale for the commitment_analysis MEDIUM at publication (cited `:648-672`, actual `:667-691`) — merge drift, not fabrication; the substance is intact.
4. **Verdict-relevant asymmetry in E1 severities.** Six E1 findings of identical shape and identical dollar-impact (none) are split across MEDIUM (transfer ×2, athena CW, commitment, monitoring, aurora) and LOW (ec2, ebs, opensearch, msk ×2) with no stated principle. Recommend a single severity for the class in the follow-up fix run.

---

## Honesty-invariant statement

**HELD.** Verified per-service: every STALE-banner service is CONFIRMED-dominant (ami 8/0, lightsail 7/0, eks 8/2, batch 7/0, apprunner 6/1, aurora 6/1, dynamodb 6/2, redshift 6/2, elasticache 8/0, opensearch 9/1, msk 7/2, transfer 4/2, dms 8/0, glue 7/0, step_functions 6/0, athena 4/2, quicksight 6/0, network 7/1, network_cost 6/1, cloudfront 5/0, api_gateway 4/1, mediastore 4/0, sagemaker 9/2, bedrock 6/4, monitoring 5/1, workspaces 8/1, commitment 6/4), and the no-banner services (ec2, ebs, rds, s3, file_systems) are CONFIRMED-dominant too. Tally arithmetic checks out: 219 CONFIRMED + 35 NEW = 254 (transfer's "(Plus)" fifth item is correctly excluded from its stated 4). No STALE-banner service is NEW-dominant, and nothing false was found hiding in the CONFIRMED lists during Pass 2 sampling.

**Caveat:** the invariant as formulated only tests for over-claiming (invented NEWs on remediated services). It is blind to the failure mode Pass 3 exposed — UNDER-claiming. The three adapters the report scored NEW: 0 (rds, s3 by cohort; elasticache at random) each yielded material missed findings up to CRITICAL. "CONFIRMED-dominant" held because the CONFIRMED claims are true, not because the adapters are clean.

---

## Final verdict

**VERDICT: REPORT_NEEDS_REVISIONS**

**The one biggest reason:** the report's headline claim — "No NEW CRITICAL or HIGH counted-dollar defect exists in any of the 34 adapters" — is falsified by independent sweeps of just three adapters: a CRITICAL counted-$ fabrication (elasticache Graviton lever counting the full node price on a $0-resolved target, up to ~$7.5k/mo/node), a HIGH live billed surcharge the adapter's own comment denies (ElastiCache Redis OSS Extended Support, SKU live-verified at $0.165/hr), a HIGH C6 budget double-spend in the flagship commitment machinery, a HIGH C11 cap failure that the lessons file names by function, and two HIGH S3 gate failures on the path protecting 100% of the S3 counted total. The audit's recall failed, not its honesty.

Secondary reasons, in order: (1) 2 of 35 NEW findings are refuted — both by the authoritative source the finding should have consulted (the EKS "fix" would have introduced a ~$73/mo/cluster overstatement); (2) the athena unconditional 0.75 factor is under-severitied by the codebase's own C9 precedent (dms 0.35 was CRITICAL); (3) the MEDIUM/LOW severity bookkeeping does not reconcile internally (13 per-service MEDIUMs vs a headline of 10, with self-contradicting reconciliation notes).

What the report gets right and can be trusted: the 219-item CONFIRMED-already-fixed backbone (25/25 sample valid, zero rubber-stamping, claimed constant-removals grep-verified), the two counted-$ MEDIUMs it did find (network VPC endpoints, bedrock token rate — both independently re-derived here), and the read-only/tally discipline. The revision needed is additive: triage the ~20 Pass-3 false negatives (EC-1, RDS-1, RDS-2, S3-1, S3-2 first), fix the two refuted findings and the severity ledger, and extend the false-negative sweep to the 31 adapters this verification did not re-sweep — three-for-three sampled adapters with material misses is not a base rate to extrapolate kindly from.

---

## Final verdict

PENDING — issued after Pass 2/3 agent results are integrated.
